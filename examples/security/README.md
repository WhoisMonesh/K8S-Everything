# Security Examples

RBAC, NetworkPolicy, and PodSecurity patterns.

## Contents
- `restrict-egress.yaml` — deny all egress, allow one service.
- `pod-security-baseline.yaml` — namespace labels for PSA baseline.
- `rbac-rolebinding.yaml` — least-privilege Role + RoleBinding.
