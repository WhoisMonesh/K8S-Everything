# Storage Examples

PV/PVC, StorageClass, and VolumeSnapshot patterns.

## Contents
- `storage-class-wait.yaml` — WaitForFirstConsumer + volume expansion.
- `storage-class-nfs.yaml` — NFS-backed dynamic-ish StorageClass.
- `volume-snapshot.yaml` — snapshot + restore a PVC.
